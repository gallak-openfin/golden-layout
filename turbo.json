{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "pipeline": {
        "build": {
            "outputs": ["out/**"]
        },
        "dev": {
            "outputs": ["out/**"]
        }
    }
}
